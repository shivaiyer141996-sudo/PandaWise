[extend]
useDefault = true

[[allowlists]]
description = "Historical PandaWise offline-queue label; local key name, not a credential"
paths = ['''apps/mobile/lib/core/offline/offline_mutation_store\.dart''']
regexes = ['''pandawise\.pending_mutations\.v1''']
